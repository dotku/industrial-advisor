{
  "name": "industrial-advisor-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run build:frontend && concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\"",
    "build:frontend": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
